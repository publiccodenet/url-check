{
    "repositories": {
        "url-check": {
            "url": "https://github.com/publiccodenet/url-check.git",
            "branch": "main",
            "ignore_files": {
                "url-check-config.json": "do not check exclude URLs",
                "url-check-demo-config.json": "do not check exclude URLs",
                "url-check-run-config.json": "do not check exclude URLs",
                "url-check.test.py": "all URLs are bogus"
            }
        }
    },
    "ignore_patterns": {
        "^http[s]\\?://twitter\\.com": "302; does not serve scripts",
        "^http[s]\\?://github\\.com/.*/edit/": "may point to yet-to-exist page",
        "^http[s]\\?://docs\\.github\\.com/": "seems blocked as DoS protection"
    },
    "transforms" : {
    }
}
